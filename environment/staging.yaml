# Frontend staging configuration for Kanopy deployment
env:
  NODE_ENV: staging
  NEXT_PUBLIC_API_URL: https://fsi-payments-backend.industrysolutions.staging.corp.mongodb.com
  NEXT_PUBLIC_CONVERTER_API_URL: https://fsi-payments-backend.industrysolutions.staging.corp.mongodb.com/api/v1/converter
  NEXT_PUBLIC_DEMO_API_URL: https://fsi-payments-backend.industrysolutions.staging.corp.mongodb.com/api/v1/demo

# envSecrets:
#   # MongoDB Configuration (will be set using helm ksec)
#   MONGODB_URI: fsi-payments-secrets
#   DATABASE_NAME: fsi-payments-secrets
#   APP_NAME: fsi-payments-secrets

# Service Account for AWS IRSA (if needed for AWS services)
# serviceAccount:
#   enabled: false
#   name: -irsa-staging